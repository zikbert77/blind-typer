{% extends 'base.html.twig' %}

{% block title %}Courses{% endblock %}

{% block body %}
    <h1>Courses</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ course.id }}</td>
            </tr>
            <tr>
                <th>GroupId</th>
                <td>{{ course.groupId }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ course.title }}</td>
            </tr>
            <tr>
                <th>TextBody</th>
                <td>{{ course.textBody }}</td>
            </tr>
            <tr>
                <th>ParsedText</th>
                <td>{{ course.parsedText }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ course.createdAt ? course.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ course.updatedAt ? course.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('courses_index') }}">back to list</a>

    <a href="{{ path('courses_edit', {'id': course.id}) }}">edit</a>

    {{ include('courses/_delete_form.html.twig') }}
{% endblock %}
