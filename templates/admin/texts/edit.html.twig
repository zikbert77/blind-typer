{% extends 'layouts/base.html.twig' %}

{% block title %}Edit Texts{% endblock %}

{% block one_row_content %}
    <div class="content">
        <a href="{{ path('texts_index') }}">&Lt;back to list</a>
        <hr>
        <h1>Edit Texts</h1>

        <div class="text-form">
            {{ include('admin/texts/_form.html.twig', {'button_label': 'Update'}) }}
        </div>

        {{ include('admin/texts/_delete_form.html.twig') }}
    </div>
{% endblock %}
