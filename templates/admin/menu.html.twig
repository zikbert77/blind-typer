<div class="rounded-block left-main-block">
    <br>
    <div class="content">
        <a href="{{ path('adminOverview') }}" class="btn btn-block">Overview</a>
        <a href="{{ path('admin_utils') }}" class="btn btn-block">Utils</a>
        <a href="{{ path('courses_index') }}" class="btn btn-block">Courses</a>
        <a href="{{ path('texts_index') }}" class="btn btn-block">Texts</a>
        <a href="{{ path('user_index') }}" class="btn btn-block">User</a>
    </div>
</div>